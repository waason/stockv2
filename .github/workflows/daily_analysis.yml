name: Daily Stock Analysis

on:
  schedule:
    - cron: '0 9 * * 1-5' # 台北時間週一到週五下午 5:00 (UTC 9:00)
  workflow_dispatch:
    inputs:
      stock_id:
        description: '輸入要分析的股票代號'
        required: true
        default: '2330'

jobs:
  analyze:
    runs-on: ubuntu-latest

    steps:
    - name: 檢出代碼
      uses: actions/checkout@v4

    - name: 設定 Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: 安裝依賴
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements-ci.txt

    - name: 執行股票分析
      run: |
        python github_run.py --stock_id "${{ github.event.inputs.stock_id || '2330' }}"

    - name: 提交報告到倉庫
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add reports/
        git diff --staged --quiet || git commit -m "📊 Update stock analysis reports - $(date +'%Y-%m-%d %H:%M:%S')"
        git push
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: 上傳報告為 Artifact (備份)
      uses: actions/upload-artifact@v4
      with:
        name: stock-reports
        path: reports/
